import { FiltroVestidoPipe } from './filtro-vestido.pipe';

describe('FiltroVestidoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVestidoPipe();
    expect(pipe).toBeTruthy();
  });
});
